#include "types.h"
#include "stat.h"
#include "user.h"

int
main(int argc, char *argv[])
{
	/*
	uint old_mask;
	uint new_mask;
	uint mask = 2020;
	printf(1, "%d\n", mask);

  	old_mask = sigprocmask(mask);
  	printf(1, "%d\n", old_mask);

  	new_mask = getpid();
  	printf(1, "%d\n", new_mask);
	*/
	
  exit();
}
